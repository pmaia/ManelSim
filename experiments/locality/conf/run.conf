initializer=simulation.BeefsLocalitySimulationInitializer
summarizer=simulation.BeefsLocalitySimulationSummarizer

simulation_start=0
simulation_end=9223372036854

fs_trace_file=/local/thiagoepdc/workspace/ManelSim/experiments/locality/data/jurupoca.filtered.cleaned.cat

num_machines=10
# Data placement police. Possible values are random and co-random
placement_police=random

# The time in seconds that a machine must remain idle before sleep. 
to_sleep_timeout=900

# The number of replicas that must be created per file.
replication_level=2

# The time in seconds that the metadata server must wait after a changed file is closed before update its replicas 
# contents.
time_to_coherence=900

# The time in seconds that the metadata server must wait after a file is deleted before actually delete its replicas.
time_to_delete_replicas=900

transition_duration=2500

# Indicates if BeeFS can wake up machines that are in some sleeping state. Possible values are true and false.
wake_on_lan=false

# Possible values are sweet_home and homeless
user_migration_algorithm=sweet_home

# If the use do not perform any operation during user_inactivity_delay
# seconds it may migrate  
user_inactivity_delay=3600
# Give migration probability [0, 1] after inactivity delay
user_migration_probability=0.5
